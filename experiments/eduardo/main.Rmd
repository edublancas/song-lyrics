---
title: "R Notebook"
output: html_notebook
---

```{r}
library(feather)
library(dplyr)
```

```{r}
tracks <- read_feather('data/transform/songs_data.feather')
lyrics <- read_feather('data/transform/mxm_embeddings.feather')
```

```{r}
# join songs data with embeddings
df <- left_join(lyrics, tracks, by='track_id')
dim(df)
```

```{r}
```



